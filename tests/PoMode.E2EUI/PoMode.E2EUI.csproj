<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\PoMode.Shared\PoMode.Shared.csproj" />
    <!-- For ModelCatalog only: the client-delegated fixture seeds its models directory from the
         same pinned URLs and SHA-256 hashes the server uses, so they can never drift apart. -->
    <ProjectReference Include="..\..\src\PoMode.API\PoMode.API.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="Microsoft.Playwright" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>
  <ItemGroup>
    <Compile Include="..\TestCommon\TestAudio.cs" Link="TestAudio.cs" />
    <Compile Include="..\TestCommon\TestPaths.cs" Link="TestPaths.cs" />
    <Compile Include="..\TestCommon\BasicPitchFixture.cs" Link="BasicPitchFixture.cs" />
    <Content Include="..\TestCommon\basic-pitch-decoder.fixture.json" Link="basic-pitch-decoder.fixture.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>
</Project>
