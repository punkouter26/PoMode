<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\PoMode.API\PoMode.API.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.TimeProvider.Testing" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <!-- Windows Media Foundation MP3 encoder for the model-accuracy sample song; the test
         falls back to WAV at runtime on machines without the encoder. -->
    <PackageReference Include="NAudio.Wasapi" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>
  <ItemGroup>
    <Compile Include="..\TestCommon\TestAudio.cs" Link="TestAudio.cs" />
    <Compile Include="..\TestCommon\TestPaths.cs" Link="TestPaths.cs" />
  </ItemGroup>
</Project>
